import * as React from "react"
import "./Sidebar.css"

export default function Sidebar() {
  return (
    <section className="sidebar">
      {/* sidebar will have two states, hidden or not hidden */}

      {/* non-hidden text will display shopping cart info */}

        {/* we need two forms, student name, and student email a checkbox to agree to terms */}
    </section>
  )
}